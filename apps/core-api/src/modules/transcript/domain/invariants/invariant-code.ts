/**
 * Stable identifiers for the Transcript context's invariants. These are the
 * guarantees the evidence spine rests on — the Shared Kernel's `EvidenceRef`
 * assumes them.
 */
export type TranscriptInvariantCode = 'T1' | 'T2' | 'T3' | 'T4' | 'T5' | 'T6' | 'T7' | 'T8';

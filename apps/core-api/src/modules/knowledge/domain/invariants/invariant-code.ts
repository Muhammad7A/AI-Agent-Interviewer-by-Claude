/**
 * Stable identifiers for the Knowledge context's invariants. They extend the
 * Shared Kernel's substrate invariants (N1–N5, E1) with rules specific to this
 * context's aggregates. Enforcing logic lives in factories/guards (not here).
 */
export type KnowledgeInvariantCode = 'K1' | 'K2' | 'K3' | 'C1' | 'C2';

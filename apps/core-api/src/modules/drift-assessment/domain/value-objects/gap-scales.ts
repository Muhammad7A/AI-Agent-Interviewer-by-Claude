/** How damaging an alignment gap or drift finding is. */
export type GapSeverity = 'Low' | 'Moderate' | 'High' | 'Critical';

/** How pervasive a gap is across the organization. */
export type GapFrequency = 'Isolated' | 'Occasional' | 'Widespread' | 'Systemic';
